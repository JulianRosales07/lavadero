# ---------------------------------------------------------------
# URL del backend Express
#
# Producción (Render):  https://lavadero-s88q.onrender.com
# Desarrollo local:     http://localhost:4000
#
# Es una variable NEXT_PUBLIC_*, así que se incrusta en el bundle
# al compilar: si la cambias hay que reiniciar `pnpm dev`
# (o volver a construir con `pnpm build`).
# ---------------------------------------------------------------
NEXT_PUBLIC_API_URL=http://localhost:4000
